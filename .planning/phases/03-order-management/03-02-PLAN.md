---
phase: 03-order-management
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified: [src/lib/orders.ts, src/app/api/admin/orders/[id]/route.ts, src/components/OrderBoard.tsx, src/components/OrderStatusEditor.tsx, src/app/admin/orders/page.tsx]
autonomous: true
requirements: [ORD-02, ORD-03]
user_setup: []

must_haves:
  truths:
    - "Orders can be assigned and moved through a clear status workflow"
    - "Status progression is visible in real time for admins"
    - "Internal state includes Pending, Confirmed, Delivered, and Paid"
  artifacts:
    - path: "src/app/api/admin/orders/[id]/route.ts"
      provides: "Order update endpoint for assignment and status changes"
      exports: ["PATCH"]
    - path: "src/components/OrderBoard.tsx"
      provides: "Order status board/list UI"
      exports: ["OrderBoard"]
---

<objective>
Add internal assignment and order status progression tools for admin operations.

Purpose: Deliver ORD-02 and ORD-03 by turning order data into a manageable workflow
Output: Assignment/status update API and dashboard interactions
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add Internal Order Update Helpers</name>
  <files>src/lib/orders.ts, src/app/api/admin/orders/[id]/route.ts</files>
  <action>
Implement helper functions and API support for:
- assignment changes
- status transitions
- validation around allowed transitions
  </action>
</task>

<task type="auto">
  <name>Task 2: Build Order Board and Status Editing UI</name>
  <files>src/components/OrderBoard.tsx, src/components/OrderStatusEditor.tsx, src/app/admin/orders/page.tsx</files>
  <action>
Add an admin-friendly order workflow UI with clear status progression and assignment controls.
  </action>
</task>

</tasks>
