---
phase: 03-order-management
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified: [src/lib/order-parser.ts, src/app/api/admin/orders/parse/route.ts, src/components/ManualOrderParser.tsx, src/app/admin/orders/page.tsx]
autonomous: true
requirements: [ORD-04]
user_setup: []

must_haves:
  truths:
    - "Admins can paste raw order text and get parsed item suggestions"
    - "Parsed results can be reviewed and corrected before creating the order"
    - "Unknown lines or ambiguous matches are surfaced clearly"
  artifacts:
    - path: "src/lib/order-parser.ts"
      provides: "Raw text parsing helpers"
      exports: ["parseManualOrderText"]
    - path: "src/app/api/admin/orders/parse/route.ts"
      provides: "Manual order parsing endpoint"
      exports: ["POST"]
    - path: "src/components/ManualOrderParser.tsx"
      provides: "Admin parser review UI"
      exports: ["ManualOrderParser"]
---

<objective>
Add raw text parsing for manual order entry with human review before save.

Purpose: Deliver ORD-04 by accelerating offline/manual order capture without sacrificing correctness
Output: Parsing helper, parse API, and review UI in the admin orders workflow
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Implement Manual Order Parsing Logic</name>
  <files>src/lib/order-parser.ts, src/app/api/admin/orders/parse/route.ts</files>
  <action>
Implement a first-pass parser that:
- extracts candidate products and quantities
- matches known products heuristically
- reports unmatched or ambiguous lines clearly
  </action>
</task>

<task type="auto">
  <name>Task 2: Add Parser Review UI to Admin Orders</name>
  <files>src/components/ManualOrderParser.tsx, src/app/admin/orders/page.tsx</files>
  <action>
Add a paste-review-correct workflow for manual order text inside the admin order page.
  </action>
</task>

</tasks>
