---
phase: 03-order-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [prisma/schema.prisma, prisma/migrations/, src/types/order.ts, src/lib/orders.ts, src/app/api/admin/orders/route.ts, src/app/admin/orders/page.tsx]
autonomous: true
requirements: [ORD-01, ORD-02]
user_setup: []

must_haves:
  truths:
    - "All orders from website or manual entry are captured in the same system"
    - "Admins can view a unified order list with current status and assignment fields"
    - "Admins can create manual orders without going through the public checkout flow"
  artifacts:
    - path: "src/app/admin/orders/page.tsx"
      provides: "Admin order management dashboard"
      min_lines: 40
    - path: "src/app/api/admin/orders/route.ts"
      provides: "Admin order listing and manual creation endpoint"
      exports: ["GET", "POST"]
    - path: "src/lib/orders.ts"
      provides: "Expanded internal order management helpers"
  key_links:
    - from: "src/app/admin/orders/page.tsx"
      to: "/api/admin/orders"
      via: "dashboard data fetch"
      pattern: "api/admin/orders"
---

<objective>
Create the initial admin order management foundation with a unified dashboard and manual order creation.

Purpose: Deliver ORD-01 and ORD-02 by making all orders visible and manageable in one internal surface
Output: Admin order list, manual order API, and supporting order model extensions
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Extend Order Model for Internal Workflow Fields</name>
  <files>prisma/schema.prisma, prisma/migrations/, src/types/order.ts</files>
  <action>
Add the minimum internal workflow fields required for admin management:
- assignment owner
- source (`WEB` vs `MANUAL`)
- internal status helpers needed by the dashboard
Update order types accordingly.
  </action>
</task>

<task type="auto">
  <name>Task 2: Create Admin Order API and Helpers</name>
  <files>src/lib/orders.ts, src/app/api/admin/orders/route.ts</files>
  <action>
Implement internal order list and manual order creation helpers.
- list orders for admin use
- create manual orders
- reuse inventory/order validation behavior where appropriate
  </action>
</task>

<task type="auto">
  <name>Task 3: Build Admin Orders Dashboard</name>
  <files>src/app/admin/orders/page.tsx</files>
  <action>
Add the first admin order management page.
- show current orders
- display source, assignment, and status
- expose entry point for manual order creation
  </action>
</task>

</tasks>
