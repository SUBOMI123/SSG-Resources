---
phase: 02-e-commerce-website
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified: [src/lib/paystack.ts, src/app/api/paystack/initialize/route.ts, src/app/api/paystack/verify/route.ts, src/app/api/paystack/webhook/route.ts, src/app/checkout/page.tsx, src/lib/orders.ts, src/types/order.ts]
autonomous: true
requirements: [WEB-04]
user_setup:
  - item: "Set PAYSTACK_SECRET_KEY and PAYSTACK_PUBLIC_KEY in local/prod environment"
    why: "Required for live payment initialization and verification"
    action: "Add the keys to .env.local and deployment environment variables"

must_haves:
  truths:
    - "Customers can initiate secure payment through Paystack after checkout"
    - "Server-side verification updates the order only after Paystack confirms payment"
    - "Webhook or callback handling is idempotent"
  artifacts:
    - path: "src/lib/paystack.ts"
      provides: "Paystack initialization and verification helpers"
      exports: ["initializePaystackTransaction", "verifyPaystackTransaction", "validatePaystackWebhook"]
    - path: "src/app/api/paystack/initialize/route.ts"
      provides: "Paystack transaction initialization endpoint"
      exports: ["POST"]
    - path: "src/app/api/paystack/verify/route.ts"
      provides: "Paystack verification endpoint"
      exports: ["POST"]
    - path: "src/app/api/paystack/webhook/route.ts"
      provides: "Paystack webhook endpoint"
      exports: ["POST"]
  key_links:
    - from: "src/app/checkout/page.tsx"
      to: "/api/paystack/initialize"
      via: "payment start request"
      pattern: "paystack/initialize"
    - from: "src/app/api/paystack/verify/route.ts"
      to: "src/lib/orders.ts"
      via: "mark order paid"
      pattern: "markOrderAsPaid"
---

<objective>
Add Paystack payment initialization, verification, and state synchronization for checkout orders.

Purpose: Deliver WEB-04 by handing checkout orders to Paystack and only treating them as paid after server verification
Output: Paystack integration endpoints and order payment synchronization
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/02-e-commerce-website/02-CONTEXT.md
@.planning/phases/02-e-commerce-website/02-RESEARCH.md
@.planning/phases/02-e-commerce-website/02-02-PLAN.md
@.planning/PROJECT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Paystack Server Helpers</name>
  <files>src/lib/paystack.ts, src/types/order.ts</files>
  <read_first>
    - src/lib/orders.ts
    - .planning/PROJECT.md
  </read_first>
  <action>
Create a focused Paystack integration layer.

- Initialize transactions from order totals and customer details
- Verify transaction references on the server
- Validate webhook signatures
- Keep the helper isolated from route concerns
  </action>
  <verify>
    <automated>test -f src/lib/paystack.ts && grep -q "initializePaystackTransaction" src/lib/paystack.ts</automated>
  </verify>
  <done>
    - Paystack helper functions exist for initialization, verification, and webhook validation
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Paystack API Routes and Order Payment Updates</name>
  <files>src/app/api/paystack/initialize/route.ts, src/app/api/paystack/verify/route.ts, src/app/api/paystack/webhook/route.ts, src/lib/orders.ts</files>
  <read_first>
    - src/app/api/checkout/route.ts
    - src/lib/orders.ts
  </read_first>
  <action>
Implement payment endpoints and order state updates.

- Create initialize endpoint for checkout page usage
- Create verify endpoint for callback/finalization
- Create webhook endpoint for asynchronous confirmation
- Add order update helpers for paid/failed payment outcomes
- Make updates idempotent by transaction reference
  </action>
  <verify>
    <automated>test -f src/app/api/paystack/initialize/route.ts && test -f src/app/api/paystack/verify/route.ts && test -f src/app/api/paystack/webhook/route.ts</automated>
  </verify>
  <done>
    - API endpoints exist for payment lifecycle handling
    - Order payment state updates are server-owned and idempotent
  </done>
</task>

<task type="auto">
  <name>Task 3: Connect Checkout UI to Paystack</name>
  <files>src/app/checkout/page.tsx</files>
  <read_first>
    - src/app/checkout/page.tsx
    - src/lib/paystack.ts
  </read_first>
  <action>
Update checkout to hand successful order creation into Paystack.

- Request payment initialization after order creation
- Redirect or invoke the Paystack checkout flow with returned authorization info
- Handle callback/verification outcomes cleanly in the UI
  </action>
  <verify>
    <automated>grep -q "paystack" src/app/checkout/page.tsx</automated>
  </verify>
  <done>
    - Checkout can move from order creation into payment
    - Customer-visible error handling exists for failed initialization/verification
  </done>
</task>

</tasks>
