---
phase: 02-e-commerce-website
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [prisma/schema.prisma, prisma/migrations/, src/lib/catalog.ts, src/types/catalog.ts, src/app/page.tsx, src/app/products/page.tsx, src/app/products/[slug]/page.tsx]
autonomous: true
requirements: [WEB-01, WEB-02]
user_setup: []

must_haves:
  truths:
    - "Customers can browse a public catalog of available products with descriptions and pricing"
    - "The public website presents the business professionally on both desktop and mobile"
    - "Catalog pages read from the real product inventory created in Phase 1"
  artifacts:
    - path: "src/app/products/page.tsx"
      provides: "Public catalog listing page"
      min_lines: 40
    - path: "src/app/products/[slug]/page.tsx"
      provides: "Product detail page with quantity selection entry point"
      min_lines: 40
    - path: "src/lib/catalog.ts"
      provides: "Catalog queries and product shaping for storefront pages"
      exports: ["listCatalogProducts", "getCatalogProductBySlug"]
    - path: "src/types/catalog.ts"
      provides: "Catalog-facing product types"
  key_links:
    - from: "src/app/products/page.tsx"
      to: "src/lib/catalog.ts"
      via: "server-side catalog query"
      pattern: "listCatalogProducts"
    - from: "src/app/products/[slug]/page.tsx"
      to: "src/lib/catalog.ts"
      via: "product detail lookup"
      pattern: "getCatalogProductBySlug"
---

<objective>
Build the public storefront foundation and catalog browsing experience using the existing Phase 1 product inventory as the data source.

Purpose: Deliver WEB-01 and WEB-02 by making products discoverable on a professional public website
Output: Public home/catalog/product pages backed by real inventory data
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/02-e-commerce-website/02-CONTEXT.md
@.planning/phases/02-e-commerce-website/02-RESEARCH.md
@.planning/phases/01-inventory-tracking/01-CONTEXT.md
@src/app/layout.tsx
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Data Model for Public Catalog Slugs and Product Presentation</name>
  <files>prisma/schema.prisma, prisma/migrations/</files>
  <read_first>
    - prisma/schema.prisma
    - src/db/seed.ts
  </read_first>
  <action>
Update the Prisma `Product` model so storefront pages can address products by slug and expose them safely on the public website.

- Add a unique `slug` field
- Add an `is_active` boolean to control public visibility
- Create a migration for the new fields
- Update seed data expectations so seeded products can render on the storefront
  </action>
  <verify>
    <automated>grep -q "slug" prisma/schema.prisma && grep -q "is_active" prisma/schema.prisma</automated>
  </verify>
  <done>
    - Product model has a unique slug
    - Product model supports public visibility
    - Migration exists for the schema extension
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Catalog Query Layer and Types</name>
  <files>src/lib/catalog.ts, src/types/catalog.ts</files>
  <read_first>
    - src/lib/inventory.ts
    - src/types/inventory.ts
  </read_first>
  <action>
Create a storefront-focused catalog query layer that reads active products from Prisma and returns only public-safe fields.

- Add catalog result types
- Provide a function to list active products with stock hints
- Provide a function to fetch a single product by slug
- Ensure unavailable or inactive products do not appear publicly
  </action>
  <verify>
    <automated>test -f src/lib/catalog.ts && test -f src/types/catalog.ts && grep -q "listCatalogProducts" src/lib/catalog.ts</automated>
  </verify>
  <done>
    - Catalog library returns public-facing product data
    - Product detail lookup is slug-based
    - Types are reusable by storefront pages
  </done>
</task>

<task type="auto">
  <name>Task 3: Build Public Website and Catalog Pages</name>
  <files>src/app/page.tsx, src/app/products/page.tsx, src/app/products/[slug]/page.tsx</files>
  <read_first>
    - src/app/page.tsx
    - src/app/globals.css
    - src/lib/catalog.ts
  </read_first>
  <action>
Create the public website experience with a polished homepage, catalog listing, and product detail page.

- Upgrade the homepage to present the business clearly and link into shopping
- Add `/products` with responsive product cards, pricing, and availability
- Add `/products/[slug]` with product detail, quantity selector, and checkout entry action
- Keep the visual language aligned with the existing warm editorial styling
  </action>
  <verify>
    <automated>test -f src/app/products/page.tsx && test -f 'src/app/products/[slug]/page.tsx' && grep -q "products" src/app/page.tsx</automated>
  </verify>
  <done>
    - Public website has a professional storefront presentation
    - Customers can browse products and open a detail page
    - Pages render real catalog data from Prisma
  </done>
</task>

</tasks>
