---
phase: 02-e-commerce-website
plan: 04
type: execute
wave: 3
depends_on: [02-02, 02-03]
files_modified: [src/app/checkout/success/page.tsx, src/app/orders/[reference]/page.tsx, src/app/api/orders/[reference]/route.ts, src/lib/orders.ts, src/components/OrderStatusCard.tsx, src/components/OrderReceipt.tsx]
autonomous: true
requirements: [WEB-05, WEB-06]
user_setup: []

must_haves:
  truths:
    - "Customers see a clear order confirmation after successful payment"
    - "Customers can look up order status using their order reference"
    - "Status values align with the roadmap flow Pending → Confirmed → Delivered"
  artifacts:
    - path: "src/app/checkout/success/page.tsx"
      provides: "Post-payment confirmation page"
      min_lines: 30
    - path: "src/app/orders/[reference]/page.tsx"
      provides: "Customer order tracking page"
      min_lines: 30
    - path: "src/app/api/orders/[reference]/route.ts"
      provides: "Public order lookup API"
      exports: ["GET"]
    - path: "src/components/OrderStatusCard.tsx"
      provides: "Reusable order status display"
      exports: ["OrderStatusCard"]
  key_links:
    - from: "src/app/orders/[reference]/page.tsx"
      to: "/api/orders/[reference]"
      via: "order lookup fetch"
      pattern: "api/orders"
    - from: "src/app/checkout/success/page.tsx"
      to: "src/lib/orders.ts"
      via: "confirmation payload"
      pattern: "getOrderByReference"
---

<objective>
Finish the customer-facing purchase flow with confirmation and tracking.

Purpose: Deliver WEB-05 and WEB-06 by showing confirmation after payment and exposing a simple order-tracking experience
Output: Confirmation page, order lookup API, and customer tracking page
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/02-e-commerce-website/02-CONTEXT.md
@.planning/phases/02-e-commerce-website/02-02-PLAN.md
@.planning/phases/02-e-commerce-website/02-03-PLAN.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Public Order Lookup and Status Helpers</name>
  <files>src/lib/orders.ts, src/app/api/orders/[reference]/route.ts</files>
  <read_first>
    - src/lib/orders.ts
    - .planning/ROADMAP.md
  </read_first>
  <action>
Expose a narrow customer-safe order lookup flow.

- Add lookup by order reference
- Return only public-safe fields for confirmation/tracking
- Keep the status vocabulary aligned to roadmap expectations
  </action>
  <verify>
    <automated>test -f 'src/app/api/orders/[reference]/route.ts' && grep -q "getOrderByReference" src/lib/orders.ts</automated>
  </verify>
  <done>
    - Public order lookup API exists
    - Lookup results are safe for customers and omit internal-only data
  </done>
</task>

<task type="auto">
  <name>Task 2: Build Confirmation and Tracking UI</name>
  <files>src/app/checkout/success/page.tsx, src/app/orders/[reference]/page.tsx, src/components/OrderStatusCard.tsx, src/components/OrderReceipt.tsx</files>
  <read_first>
    - src/app/checkout/page.tsx
    - src/app/globals.css
  </read_first>
  <action>
Create customer-facing post-purchase surfaces.

- Add a success/confirmation page after payment
- Add an order-tracking page keyed by order reference
- Display order summary, payment state, and delivery progress
- Reuse a status card component for consistency
  </action>
  <verify>
    <automated>test -f src/app/checkout/success/page.tsx && test -f 'src/app/orders/[reference]/page.tsx' && test -f src/components/OrderStatusCard.tsx</automated>
  </verify>
  <done>
    - Customers see confirmation after successful payment
    - Customers can check status by order reference
    - Tracking UI is mobile friendly and consistent with the storefront
  </done>
</task>

</tasks>
