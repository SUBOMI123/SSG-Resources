---
phase: 02-e-commerce-website
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified: [prisma/schema.prisma, prisma/migrations/, src/types/order.ts, src/lib/orders.ts, src/app/api/checkout/route.ts, src/app/checkout/page.tsx, src/components/CheckoutForm.tsx, src/components/CartSummary.tsx]
autonomous: true
requirements: [WEB-03]
user_setup: []

must_haves:
  truths:
    - "Customers can select products and quantities and submit an online order"
    - "Checkout validates product availability against live inventory before order creation"
    - "Guest checkout captures customer and delivery details without requiring auth"
  artifacts:
    - path: "src/lib/orders.ts"
      provides: "Order creation and inventory reservation business logic"
      exports: ["createCheckoutOrder", "getOrderByReference"]
    - path: "src/app/api/checkout/route.ts"
      provides: "Checkout API endpoint"
      exports: ["POST"]
    - path: "src/app/checkout/page.tsx"
      provides: "Customer checkout page"
      min_lines: 40
    - path: "src/components/CheckoutForm.tsx"
      provides: "Checkout form for customer/order details"
      exports: ["CheckoutForm"]
  key_links:
    - from: "src/app/api/checkout/route.ts"
      to: "src/lib/orders.ts"
      via: "order creation call"
      pattern: "createCheckoutOrder"
    - from: "src/lib/orders.ts"
      to: "src/lib/inventory.ts"
      via: "inventory reservation"
      pattern: "reserveInventory"
---

<objective>
Create the guest checkout and order creation flow on top of the public catalog.

Purpose: Deliver WEB-03 by enabling customers to submit an order with selected products and quantities
Output: Checkout page, API, and order persistence with inventory reservation
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/02-e-commerce-website/02-CONTEXT.md
@.planning/phases/02-e-commerce-website/02-01-PLAN.md
@.planning/phases/01-inventory-tracking/01-CONTEXT.md
@src/lib/inventory.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Order Persistence for Web Checkout</name>
  <files>prisma/schema.prisma, prisma/migrations/, src/types/order.ts</files>
  <read_first>
    - prisma/schema.prisma
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
Extend the schema with the minimum order entities required for guest checkout and future Phase 3 reuse.

- Add `Order` and `OrderItem` models
- Capture customer name, phone/email, delivery address, order reference, payment status, and fulfillment status
- Snapshot product name/price at order time
- Add TypeScript order contracts in `src/types/order.ts`
  </action>
  <verify>
    <automated>grep -q "model Order" prisma/schema.prisma && grep -q "model OrderItem" prisma/schema.prisma && test -f src/types/order.ts</automated>
  </verify>
  <done>
    - Prisma schema can persist web orders and line items
    - Order contracts exist for checkout/business logic
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Checkout Business Logic and API</name>
  <files>src/lib/orders.ts, src/app/api/checkout/route.ts</files>
  <read_first>
    - src/lib/inventory.ts
    - src/lib/db.ts
  </read_first>
  <action>
Implement checkout order creation with inventory validation and reservation.

- Validate products and requested quantities
- Create a unique order reference
- Reserve inventory atomically for all line items
- Persist the order as pending payment
- Return the order reference and payment-init payload placeholder
  </action>
  <verify>
    <automated>test -f src/lib/orders.ts && test -f src/app/api/checkout/route.ts && grep -q "createCheckoutOrder" src/lib/orders.ts</automated>
  </verify>
  <done>
    - Checkout API creates pending orders
    - Inventory is reserved before payment
    - Failure paths return clear validation errors
  </done>
</task>

<task type="auto">
  <name>Task 3: Build Checkout UI</name>
  <files>src/app/checkout/page.tsx, src/components/CheckoutForm.tsx, src/components/CartSummary.tsx</files>
  <read_first>
    - src/app/products/[slug]/page.tsx
    - src/app/globals.css
  </read_first>
  <action>
Build the guest checkout page and components.

- Add cart/summary presentation for selected items
- Add customer contact and delivery fields
- Submit to `/api/checkout`
- Present loading, stock validation, and submission error states clearly
  </action>
  <verify>
    <automated>test -f src/app/checkout/page.tsx && test -f src/components/CheckoutForm.tsx && test -f src/components/CartSummary.tsx</automated>
  </verify>
  <done>
    - Customers can review their order and submit checkout details
    - Checkout page works on mobile and desktop
    - Checkout UI integrates with the server API
  </done>
</task>

</tasks>
