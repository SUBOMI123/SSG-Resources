---
phase: 06-partial-payments-and-scalable-order-workspace
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [prisma/schema.prisma, prisma/migrations/20260420062000_partial_payments/migration.sql, src/types/order.ts, src/lib/orders.ts, src/app/api/admin/orders/[id]/payments/route.ts]
autonomous: true
requirements: [PAY-01, PAY-02]
user_setup: []

must_haves:
  truths:
    - "Orders can store amount paid, balance left, and fully paid state"
    - "More than one payment can be recorded against the same order"
    - "Admin cannot mark an order as fully paid while there is still a balance left"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Partial-payment data model"
      exports: ["PaymentStatus", "Order", "OrderPayment"]
    - path: "src/lib/orders.ts"
      provides: "Order creation, retrieval, update, and payment recording with balance tracking"
      exports: ["createCheckoutOrder", "createManualOrder", "getOrderByReference", "listAdminOrders", "updateAdminOrder", "recordOrderPayment"]
    - path: "src/app/api/admin/orders/[id]/payments/route.ts"
      provides: "Admin route to record a payment against an order"
      exports: ["POST"]
---

<objective>
Ship the backend foundation for partial payments so the business can track money received over time instead of using a single paid or unpaid flag.

Purpose: Support real-world installment and half-payment workflows
Output: Updated schema, migration, API route, and order logic that records payment history and keeps balances accurate
</objective>
