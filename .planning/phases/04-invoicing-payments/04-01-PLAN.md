---
phase: 04-invoicing-payments
plan: 01
type: execute
wave: 1
depends_on: [03-03]
files_modified: [src/lib/documents.ts, src/components/InvoiceDocument.tsx, src/components/WaybillDocument.tsx, src/components/PrintDocumentButton.tsx, src/app/documents/invoice/[reference]/page.tsx, src/app/documents/waybill/[reference]/page.tsx, src/components/OrderBoard.tsx, src/app/orders/[reference]/page.tsx, src/app/globals.css]
autonomous: true
requirements: [INV-01, INV-02, INV-03]
user_setup: []

must_haves:
  truths:
    - "Invoices can be generated instantly from existing orders"
    - "Waybills can be generated instantly from existing orders"
    - "Users can print or save documents at any time from stable document URLs"
  artifacts:
    - path: "src/lib/documents.ts"
      provides: "Shared invoice and waybill document builders"
      exports: ["buildInvoiceDocument", "buildWaybillDocument"]
    - path: "src/app/documents/invoice/[reference]/page.tsx"
      provides: "Printable invoice page"
      exports: ["default"]
    - path: "src/app/documents/waybill/[reference]/page.tsx"
      provides: "Printable waybill page"
      exports: ["default"]
---

<objective>
Generate operational invoice and waybill documents directly from order data.

Purpose: Deliver INV-01, INV-02, and INV-03 with the smallest useful document system
Output: Shared document builders, printable document pages, and entry points from existing order/admin screens
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add Shared Document Builders</name>
  <files>src/lib/documents.ts</files>
  <action>
Create reusable invoice and waybill models derived from existing order records so document rendering logic stays consistent.
  </action>
</task>

<task type="auto">
  <name>Task 2: Build Printable Invoice and Waybill Pages</name>
  <files>src/components/InvoiceDocument.tsx, src/components/WaybillDocument.tsx, src/components/PrintDocumentButton.tsx, src/app/documents/invoice/[reference]/page.tsx, src/app/documents/waybill/[reference]/page.tsx, src/app/globals.css</files>
  <action>
Implement print-friendly document pages with browser print and Save as PDF support.
  </action>
</task>

<task type="auto">
  <name>Task 3: Expose Documents in Order Workflows</name>
  <files>src/components/OrderBoard.tsx, src/app/orders/[reference]/page.tsx</files>
  <action>
Add stable links to invoice and waybill pages from both admin order cards and customer order tracking.
  </action>
</task>

</tasks>
