---
phase: 00-deployment-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [docker-compose.yml, Dockerfile, .dockerignore, .env.example, README.md]
autonomous: true
requirements: []
user_setup:
  - item: "Docker Desktop installed"
    why: "Required to run docker-compose for local database"
    action: "Download from https://www.docker.com/products/docker-desktop"

must_haves:
  truths:
    - "Local development runs PostgreSQL in Docker (matches production)"
    - "npm run dev connects to local database via docker-compose"
    - "docker-compose down clears all data for clean state"
  artifacts:
    - path: "docker-compose.yml"
      provides: "PostgreSQL 17 + PgAdmin for local development"
      min_lines: 30
    - path: "Dockerfile"
      provides: "Production image build configuration"
      min_lines: 15
    - path: ".env.example"
      provides: "Template for environment variables (no secrets)"
  key_links:
    - from: "npm run dev"
      to: "docker-compose.yml"
      via: "DATABASE_URL in .env.local"
      pattern: "postgresql://.*localhost:5432"
---

<objective>
Set up Docker for local development and production image building. Enable developers to run PostgreSQL 17 locally matching production, and prepare Docker image for Vercel deployment.

Purpose: Ensure local environment matches production (same PostgreSQL version, same schema)
Output: docker-compose.yml for local dev, Dockerfile for production builds
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/00-deployment-setup/00-CONTEXT.md — D-03 (Docker Compose), D-06 (Alpine image)
@.planning/research/STACK.md — Node.js 22.x, PostgreSQL 17.x
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docker-compose.yml for Local Development</name>
  <files>docker-compose.yml</files>
  <action>
Create docker-compose.yml at project root with PostgreSQL 17 and PgAdmin:

```yaml
version: '3.8'

services:
  postgres:
    image: postgres:17-alpine
    container_name: ssg_postgres
    environment:
      POSTGRES_USER: ssg_user
      POSTGRES_PASSWORD: ssg_password
      POSTGRES_DB: ssg_dev
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ssg_user -d ssg_dev"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - ssg_network

  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: ssg_pgadmin
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@example.com
      PGADMIN_DEFAULT_PASSWORD: admin
    ports:
      - "5050:80"
    depends_on:
      - postgres
    networks:
      - ssg_network

volumes:
  postgres_data:

networks:
  ssg_network:
```

Per D-03: Uses PostgreSQL 17-alpine (matches production), data persists in docker volume.
  </action>
  <verify>
    <automated>test -f docker-compose.yml && grep -q "postgres:17" docker-compose.yml && grep -q "pgadmin" docker-compose.yml</automated>
  </verify>
  <done>
    - docker-compose.yml exists at project root
    - Defines PostgreSQL 17 service with volume persistence
    - Includes PgAdmin for database inspection
    - Health checks configured
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Production Dockerfile</name>
  <files>Dockerfile, .dockerignore</files>
  <action>
Create Dockerfile for production deployment to Vercel:

```dockerfile
# Stage 1: Build
FROM node:22-alpine AS builder
WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci

COPY . .
RUN npm run build

# Stage 2: Runtime (lean production image)
FROM node:22-alpine
WORKDIR /app

ENV NODE_ENV=production

COPY package.json package-lock.json ./
RUN npm ci --only=production

COPY --from=builder /app/.next ./.next
COPY --from=builder /app/prisma ./prisma
COPY --from=builder /app/public ./public

EXPOSE 3000
CMD ["npm", "start"]
```

Per D-06: Multi-stage build excludes dev dependencies. Alpine base keeps image small (~300MB).

Create .dockerignore to exclude unnecessary files:

```
.git
.gitignore
node_modules
.next
.env
.env.local
.DS_Store
npm-debug.log
docker-compose.yml
.planning
README.md
```
  </action>
  <verify>
    <automated>test -f Dockerfile && grep -q "FROM node:22-alpine" Dockerfile && test -f .dockerignore</automated>
  </verify>
  <done>
    - Dockerfile created with multi-stage build
    - Production image excludes dev dependencies
    - .dockerignore prevents unnecessary layer bloat
  </done>
</task>

<task type="auto">
  <name>Task 3: Create .env.example Template</name>
  <files>.env.example</files>
  <action>
Create .env.example at project root with all required variables (NO SECRETS):

```env
# Database
DATABASE_URL="postgresql://ssg_user:ssg_password@localhost:5432/ssg_dev"

# Environment
NODE_ENV="development"

# Paystack (Phase 2)
# PAYSTACK_SECRET_KEY="pk_test_..." (add in Vercel, not here)
# PAYSTACK_PUBLIC_KEY="pk_test_..."

# Auth (Phase 2)
# AUTH_SECRET="..." (add in Vercel, not here)

# Future: Sentry (Phase 1)
# SENTRY_DSN="..."
```

Per D-05: This template has no secrets. Developers copy to .env.local and fill in local values.

Add to .gitignore (ensure not already there):
```
.env.local
.env.*.local
```
  </action>
  <verify>
    <automated>test -f .env.example && grep -q "DATABASE_URL" .env.example && ! grep -q "password@" .env.example</automated>
  </verify>
  <done>
    - .env.example exists with all variable templates
    - No secrets or real credentials in template
    - .env.local is git-ignored
  </done>
</task>

<task type="auto">
  <name>Task 4: Document Local Development Setup in README.md</name>
  <files>README.md</files>
  <action>
Update README.md with getting started section (or create if missing):

```markdown
# Digital Inventory & Order Management System

A complete inventory tracking, order management, and invoicing system built with Next.js, PostgreSQL, and Paystack.

## Quick Start

### Prerequisites
- Node.js 22.x LTS
- Docker Desktop (for PostgreSQL)
- Git

### Local Development Setup

1. **Clone repository:**
   ```bash
   git clone <repo-url>
   cd SSG-Resources
   ```

2. **Install dependencies:**
   ```bash
   npm install
   ```

3. **Configure environment:**
   ```bash
   cp .env.example .env.local
   # Edit .env.local if needed (defaults work for docker-compose)
   ```

4. **Start database:**
   ```bash
   docker-compose up -d
   # Verify: pg_isready -h localhost -U ssg_user
   ```

5. **Initialize database schema:**
   ```bash
   npx prisma migrate dev
   # This creates tables and runs seed data
   ```

6. **Start development server:**
   ```bash
   npm run dev
   # Visit http://localhost:3000
   # Admin dashboard: http://localhost:3000/admin/inventory
   ```

### Common Commands

| Command | Purpose |
|---------|---------|
| `docker-compose up -d` | Start PostgreSQL in background |
| `docker-compose down` | Stop database (data persists) |
| `docker-compose down -v` | Stop + delete all data (clean slate) |
| `npx prisma studio` | Open Prisma Studio (visual database editor) |
| `npm run dev` | Start Next.js dev server |
| `npm run test` | Run tests (Phase 1+) |
| `npm run build` | Build for production |

### Database Access

- **Via PgAdmin Web UI:**
  - Visit http://localhost:5050
  - Login: admin@example.com / admin
  - Connect to postgres:5432

- **Via `psql` CLI:**
  ```bash
  psql postgresql://ssg_user:ssg_password@localhost:5432/ssg_dev
  ```

### Production Deployment

Deployment is automated via GitHub Actions:
1. Push to feature branch → GitHub Actions runs tests
2. Create PR → Review code
3. Merge to main → Vercel deploys automatically
4. Visit https://your-app.vercel.app

**Environment Variables (set in Vercel Dashboard):**
- `DATABASE_URL` → Supabase connection string
- `PAYSTACK_SECRET_KEY` → Live Paystack key (Phase 2)

### Troubleshooting

**Port 5432 already in use:**
```bash
# Kill existing PostgreSQL container
docker-compose down

# Or use different port in docker-compose.yml
```

**Prisma schema out of sync:**
```bash
# Reset database to current schema
npx prisma migrate reset
```

---

For more details, see `.planning/` directory for phase documentation.
```

Add this to the existing README.md or create new if missing.
  </action>
  <verify>
    <automated>test -f README.md && grep -q "Quick Start\|docker-compose\|npm run dev" README.md</automated>
  </verify>
  <done>
    - README.md includes getting started section
    - All common commands documented
    - Database access methods explained
    - Deployment instructions clear
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docker Host → Container | Local dev database is trusted (same machine) |
| .env.local → Application | Secrets loaded from environment variables only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-01 | Information Disclosure | .env.local | mitigate | .env.local is .gitignored; never commit secrets |
| T-00-02 | Denial of Service | Docker volumes | accept | Local dev only; attacker has physical access to delete anyway |
| T-00-03 | Elevation of Privilege | docker-compose | accept | Runs as user with Docker socket access; no root privilege escalation |

</threat_model>

<verification>
**After plan completion:**
1. docker-compose.yml exists and defines PostgreSQL + PgAdmin
2. Dockerfile built successfully: `docker build -t ssg-app .`
3. Local development database accessible via both PgAdmin and CLI
4. Environment variables properly templated in .env.example
5. README.md provides clear setup instructions
6. .gitignore blocks .env.local from version control

**Manual test:**
```bash
docker-compose up -d
# Wait ~5 seconds for postgres health check
docker-compose exec postgres psql -U ssg_user -d ssg_dev -c "SELECT version();"
# Should return PostgreSQL 17 version
docker-compose down
```
</verification>

<success_criteria>
Phase 0 - Plan 01 is complete when:
✓ docker-compose.yml configured with PostgreSQL 17 + PgAdmin
✓ Dockerfile created with multi-stage build for production
✓ .dockerignore prevents unnecessary layers
✓ .env.example provides template for all environment variables
✓ README.md documents local setup and common commands
✓ .env.local is git-ignored (never appears in commits)
✓ Developers can run `docker-compose up -d` → instant PostgreSQL
✓ All documentation is clear for solo developer or future team members
</success_criteria>

<output>
After completion, create `.planning/phases/00-deployment-setup/00-01-SUMMARY.md` with:
- Docker setup verified
- Local database confirmed running
- Development environment ready for Phase 1
</output>
